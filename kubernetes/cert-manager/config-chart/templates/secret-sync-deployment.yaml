apiVersion: apps/v1
kind: Deployment
metadata:
  name: cert-manager-cloudflare-sync
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cert-manager-cloudflare-sync
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cert-manager-cloudflare-sync
    spec:
      serviceAccountName: cert-manager-cloudflare-sync
      containers:
        - name: sync
          image: busybox:1.36
          command:
            - sh
            - -ec
            - |
              while true; do
                test -s /mnt/secrets-store/api_token
                sleep 3600
              done
          volumeMounts:
            - name: secrets-store
              mountPath: /mnt/secrets-store
              readOnly: true
          resources:
            requests:
              cpu: 5m
              memory: 16Mi
            limits:
              memory: 32Mi
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: cert-manager-cloudflare
