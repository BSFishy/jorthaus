apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik
  namespace: {{ .Release.Namespace }}
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`auth.jort.haus`)
      kind: Rule
      services:
        - name: authentik-server
          port: 80
  tls: {}
